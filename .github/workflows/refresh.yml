name: refresh-hub

on:
  workflow_dispatch: {}       # triggered every 15 min by external cron (cron-job.org) via the API;
                              # GitHub's own schedule was removed — it was throttled/unreliable

permissions:
  contents: write             # allow the job to commit the updated data.json

concurrency:
  group: refresh-hub
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: python collector.py

      - name: commit updated data
        run: |
          git config user.name  "media-hub-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data.json
          if git diff --staged --quiet; then
            echo "no changes"
          else
            git commit -m "hub: refresh $(date -u +%Y-%m-%dT%H:%MZ)"
            # a manual push can land mid-run and reject ours as non-fast-forward;
            # data.json is fully regenerated each run, so rebasing onto it is safe
            for attempt in 1 2 3; do
              git pull --rebase --autostash && git push && exit 0
              echo "push attempt $attempt lost the race, retrying"
              sleep 5
            done
            exit 1
          fi
